# Claude project instructions

Before any frontend task, read `AGENTS.md` and `AI-RULE-FE.md`.

Before reviewing, proposing, or changing UI, read `DESIGN_LANGUAGE.md` in full. It is the canonical design source of truth. Use `AI-UI-HANDOFF.md` when continuing work from another conversation.

Preserve unrelated working-tree changes and verify changed frontend code with targeted ESLint and `npm run build`.
